# Shared defaults. Use --config with a small YAML override, or CLI flags.
# Project seed/max_action_steps are read from configs/spec/project.yaml.
scenario: null
model_path: null
tokenizer_path: null
adapter_path: null
output_dir: null
# Audited reference pin; local upstream source archive has no .git metadata.
upstream_reference_commit: 51bb26012cee31aea7ac26177c5ffe807026ac07
generation:
  # PROJECT-FIXED v1: visible Thought/Action, no native Qwen reasoning.
  chat_template_kwargs:
    enable_thinking: false
  max_new_tokens: 512
  max_context_tokens: 32768
  do_sample: false
  temperature: 1.0
  top_p: 1.0
sft:
  num_train_epochs: 4
  learning_rate: 1.0e-5
  effective_batch_size: 32
  max_length: 32768
  # Select these after GPU preflight; the wrapper requires explicit values.
  per_device_train_batch_size: null
  gradient_accumulation_steps: null
  lora_r: null
  lora_alpha: null
  target_modules: null
  lora_dropout: 0.05
  gradient_checkpointing: true
  save_steps: 100
  logging_steps: 10
