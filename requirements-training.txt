# D1 CPU-verified with Python 3.10.21 (macOS arm64).
# Linux GPU: same API versions; select a CUDA-compatible torch 2.8.0 build.
# Install ONLY into an independent training environment, never the teacher env.
torch==2.8.0
transformers==4.57.6
trl==1.12.0
peft==0.19.1
accelerate==1.15.0
datasets==4.8.5
# Tokenization is part of the policy/data contract.
tokenizers==0.22.2
# Project config and ShopEnv protocol client (runtime tests use FakeEnv).
PyYAML==6.0.3
httpx==0.28.1
